# visualize secret doors
> entities prototyped SolidSecretDoor visualize
# visualize active zombies
> entities with Zombie actor:controlled visualize
# visualize active nukies
> entities with NukeOperative actor:controlled visualize
# visualize mechs
> entities with MechAir visualize
# visualize borgs
> entities with BorgChassis actor:controlled visualize
# visualize smuggler's satchels
> entities with Anchorable with Clothing visualize
# visualize aghosts
> entities prototyped AdminObserver visualize

# spawn an item in your admin bag !!ADD ITEM PROTOID TO THE END OF THE COMMAND!!
> self nearby 0 named "satchel of administration" spawn:in "storagebase"
# spawn a Forensic Scanner in your admin bag
> self nearby 0 named "satchel of administration" spawn:in "storagebase" ForensicScanner

# NOTE: the follwing commands will spawn the item on the ground and as such shouldn't be used while near players
# spawn a stamp on the ground that stamps the message: Wrong Form
> self spawn:on RubberStampDenied do "vvwrite /entity/$ID/Name \"Wrong Form Stamp\"" do "vvwrite /entity/$ID/Stamp/StampedName \"Wrong Form\""
# spawn a stamp on the ground that stamps the message: Missing Stamps
> self spawn:on RubberStampDenied do "vvwrite /entity/$ID/Name \"Missing Stamps Stamp\"" do "vvwrite /entity/$ID/Stamp/StampedName \"Missing Stamps\""
# spawn a stamp on the ground that stamps the message: Delivered to ATS
> self spawn:on RubberStampQm do "vvwrite /entity/$ID/Name \"ATS Stamp\"" do "vvwrite /entity/$ID/Stamp/StampedName \"Delivered to ATS\""
# spawn a stamp on the ground that stamps the message: Acknowledged
> self spawn:on RubberStampApproved do "vvwrite /entity/$ID/Name \"Acknowledged Stamp\"" do "vvwrite /entity/$ID/Stamp/StampedName \"Acknowledged\""
